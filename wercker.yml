box: golang:latest
build:
  steps:
    - arjen/hugo-build:
        version: "0.31.1"
        theme: "peak"
deploy:
  steps:
    - gh-pages:
        token: $GIT_TOKEN
        basedir: public
        branch: master
        domain: hugo-wercker.ig.nore.me
